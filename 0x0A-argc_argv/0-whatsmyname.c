#include <stdio.h>
/**
 * main - entry point
 *@argc: number of arguments supplied to th program
 *@argv: array of strings
 * Return: always 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	printf("\n")
	return (0);
}
