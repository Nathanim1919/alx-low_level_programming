#include "main.h"
/**
 * add - adds two integers and returns the result
 * @n: the first integer to to be added
 * @m: the secon integer to be added
 * Return: their sum
 */
int add(int n, int m)
{
	int r = n + m;

	return (r);
}
